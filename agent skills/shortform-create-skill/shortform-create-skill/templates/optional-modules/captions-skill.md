# Optional Captions Module

Use only when the user selected captions.

Inputs: audio/video plus transcript or transcription provider.

Outputs: captioned MP4 or subtitle assets, depending on user choice.
