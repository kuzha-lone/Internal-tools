# Core — needed across services
python-dotenv>=1.0
httpx>=0.27
feedparser>=6.0
apscheduler>=3.10

# News engine
telethon>=1.34          # only if reading private telegram channels
python-telegram-bot>=20.0

# MCP
fastmcp>=0.2

# Trading engine (install on the box that talks to brokers, not necessarily everywhere)
# MetaTrader5>=5.0       # Windows-only, uncomment where the MT5 terminal actually runs

# Retraining (Phase 3+, not needed for the Stage 1 simulator which is stdlib-only)
# scikit-learn>=1.4
# pandas>=2.2
